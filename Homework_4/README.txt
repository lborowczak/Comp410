How to build: run "make", if it does not work, open "Makefile" and replace the values after CC = , LINKER = , and ASSEMBLER = with the gcc cross-compiler, gcc cross-linker, and gcc cross-assembler executable names, respectively.

How to run: boot "lborowczak-kernel.iso" in a virtual machine like virtualbox or qemu as a liveCD.
